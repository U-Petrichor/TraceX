# 组员4测试使用说明

## 🚀 快速开始

### 方法1：直接运行（推荐，当前环境即可）

```powershell
# 在项目根目录运行
pytest tests/test_graph_analyzer.py -v
```

**说明：** 测试文件已经自动添加了路径设置，不需要激活虚拟环境，直接运行即可！

### 方法2：如果使用虚拟环境

```powershell
# 激活虚拟环境（如果有的话）
.venv\Scripts\Activate.ps1

# 然后运行测试
pytest tests/test_graph_analyzer.py -v
```

## 📋 常用命令

```powershell
# 运行所有测试
pytest tests/test_graph_analyzer.py -v

# 运行特定模块测试
pytest tests/test_graph_analyzer.py::TestEntityExtractor -v

# 只显示失败的测试
pytest tests/test_graph_analyzer.py -v --tb=short -x

# 显示详细输出
pytest tests/test_graph_analyzer.py -v -s
```

## ✅ 当前状态

- ✅ 测试文件已修复导入问题
- ✅ 已清理多余文件
- ✅ 可以直接运行测试

## 📝 测试结果

运行测试后，你会看到：
- **46个测试通过** ✅
- **1个测试失败**（这是正常的，用于验证组员4的实现）

## 💡 提示

**不需要做任何环境配置！** 测试代码已经自动处理了路径问题，直接运行 `pytest` 命令即可。
